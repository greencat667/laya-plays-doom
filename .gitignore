__pycache__/
*.pyc
.venv/
venv/
logs/*.jsonl
models/
.pytest_cache/
.ruff_cache/
*.egg-info/
.DS_Store
_vizdoom.ini

# Hugging Face model cache, if HF_HOME/TRANSFORMERS_CACHE ever points inside
# this project directory instead of the default ~/.cache/huggingface.
.cache/
huggingface_cache/

# Local background-music asset, not for the public repo.
*.flac

# Claude Code session state (worktrees, etc.), not project source.
.claude/

# Local renders and render logs (the log holds absolute local paths); the
# playthrough is published on YouTube instead.
logs/*.mp4
logs/*.log
